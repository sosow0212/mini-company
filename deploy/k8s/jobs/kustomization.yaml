# 배치성 작업. 주기 실행(CronJob)은 로컬 APScheduler를 플랫폼 스케줄러로 대체하고(§10.2),
# 일회성 Job은 배포와 분리된 초기화 단계를 맡는다.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# overlay 없이 직접 apply해도 default로 새지 않도록 여기서 고정한다.
namespace: mini-company

resources:
  - bootstrap-job.yaml
  - worker-cronjob.yaml
